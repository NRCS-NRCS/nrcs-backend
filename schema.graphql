"""ActionLink(id, url, label, highlight)"""
type ActionLinkType {
  label: String!
  url: String!
}

type AppEnumCollection {
  DummyEnum: [AppEnumCollectionDummyEnum!]!
}

type AppEnumCollectionDummyEnum {
  key: DummyEnum!
  label: String!
}

"""
Blog(id, created_at, modified_at, created_by, modified_by, title, published_date, author, content, cover_image, featured, status, slug, department, directive)
"""
input BlogFilter {
  id: ID
  slug: StrFilterLookup
  status: StatusEnum!
  author: StrFilterLookup
  featured: BoolBaseFilterLookup
  directive: ID = null
  AND: BlogFilter
  OR: BlogFilter
  NOT: BlogFilter
  DISTINCT: Boolean
}

input BlogOrder {
  id: Ordering
  featured: Ordering
}

"""
Blog(id, created_at, modified_at, created_by, modified_by, title, published_date, author, content, cover_image, featured, status, slug, department, directive)
"""
type BlogType {
  id: ID!
  title: String!
  publishedDate: Date!
  author: String!
  content: String!
  coverImage: DjangoFileType
  featured: Boolean!
  status: StatusEnum!
  slug: String!
  department: DjangoModelType
  directive: DjangoModelType
}

input BoolBaseFilterLookup {
  """Exact match. Filter will be skipped on `null` value"""
  exact: Boolean

  """Assignment test. Filter will be skipped on `null` value"""
  isNull: Boolean

  """
  Exact match of items in a given list. Filter will be skipped on `null` value
  """
  inList: [Boolean!]
}

"""Date (isoformat)"""
scalar Date

input DateDateFilterLookup {
  """Exact match. Filter will be skipped on `null` value"""
  exact: Date

  """Assignment test. Filter will be skipped on `null` value"""
  isNull: Boolean

  """
  Exact match of items in a given list. Filter will be skipped on `null` value
  """
  inList: [Date!]

  """Greater than. Filter will be skipped on `null` value"""
  gt: Date

  """Greater than or equal to. Filter will be skipped on `null` value"""
  gte: Date

  """Less than. Filter will be skipped on `null` value"""
  lt: Date

  """Less than or equal to. Filter will be skipped on `null` value"""
  lte: Date

  """Inclusive range test (between)"""
  range: DateRangeLookup
  year: IntComparisonFilterLookup
  month: IntComparisonFilterLookup
  day: IntComparisonFilterLookup
  weekDay: IntComparisonFilterLookup
  isoWeekDay: IntComparisonFilterLookup
  week: IntComparisonFilterLookup
  isoYear: IntComparisonFilterLookup
  quarter: IntComparisonFilterLookup
}

input DateRangeLookup {
  start: Date = null
  end: Date = null
}

"""
Department(id, created_at, modified_at, created_by, modified_by, title, description, strategic_directive, contact_person_name, contact_person_email, slug)
"""
input DepartmentFilter {
  id: ID!
  slug: StrFilterLookup
  AND: DepartmentFilter
  OR: DepartmentFilter
  NOT: DepartmentFilter
  DISTINCT: Boolean
}

input DepartmentOrder {
  id: Ordering
}

"""
Department(id, created_at, modified_at, created_by, modified_by, title, description, strategic_directive, contact_person_name, contact_person_email, slug)
"""
type DepartmentType {
  id: ID!
  title: String!
  description: String!
  contactPersonName: String
  contactPersonEmail: String
  slug: String!
  strategicDirective: StrategicDirectivesType!
}

type DjangoFileType {
  name: String!
  size: Int!
  url: String!
}

input DjangoModelFilterInput {
  id: ID!
}

type DjangoModelType {
  pk: ID!
}

enum DummyEnum {
  VALUE_1
  VALUE_2
}

"""
Faq(id, created_at, modified_at, created_by, modified_by, question, answer, order_index)
"""
input FaqFilter {
  id: ID!
  orderIndex: IntComparisonFilterLookup
  AND: FaqFilter
  OR: FaqFilter
  NOT: FaqFilter
  DISTINCT: Boolean
}

input FaqOrder {
  id: Ordering
  orderIndex: Ordering
}

"""
Faq(id, created_at, modified_at, created_by, modified_by, question, answer, order_index)
"""
type FaqType {
  id: ID!
  question: String!
  answer: String!
  orderIndex: Int!
}

"""
Highlight(id, created_at, modified_at, created_by, modified_by, heading, description, image, expiry_date)
"""
input HighlightFilter {
  expiryDate: DateDateFilterLookup
  heading: String = null
  AND: HighlightFilter
  OR: HighlightFilter
  NOT: HighlightFilter
  DISTINCT: Boolean
}

input HighlightOrder {
  id: Ordering
  heading: Ordering
}

"""
Highlight(id, created_at, modified_at, created_by, modified_by, heading, description, image, expiry_date)
"""
type HighlightType {
  id: ID!
  heading: String!
  description: String!
  image: DjangoFileType
  actionLinks: [ActionLinkType!]!
  expiryDate: Date!
}

input IntComparisonFilterLookup {
  """Exact match. Filter will be skipped on `null` value"""
  exact: Int

  """Assignment test. Filter will be skipped on `null` value"""
  isNull: Boolean

  """
  Exact match of items in a given list. Filter will be skipped on `null` value
  """
  inList: [Int!]

  """Greater than. Filter will be skipped on `null` value"""
  gt: Int

  """Greater than or equal to. Filter will be skipped on `null` value"""
  gte: Int

  """Less than. Filter will be skipped on `null` value"""
  lt: Int

  """Less than or equal to. Filter will be skipped on `null` value"""
  lte: Int

  """Inclusive range test (between)"""
  range: IntRangeLookup
}

input IntRangeLookup {
  start: Int = null
  end: Int = null
}

"""
JobVacancy(id, created_at, modified_at, created_by, modified_by, title, file, position, description, number_of_vacancies, expiry_date, department, is_archived, published_at)
"""
input JobVacancyFilter {
  expiryDate: DateDateFilterLookup
  isArchived: Boolean
  AND: JobVacancyFilter
  OR: JobVacancyFilter
  NOT: JobVacancyFilter
  DISTINCT: Boolean
}

input JobVacancyOrder {
  id: Ordering
}

"""
JobVacancy(id, created_at, modified_at, created_by, modified_by, title, file, position, description, number_of_vacancies, expiry_date, department, is_archived, published_at)
"""
type JobVacancyType {
  id: ID!
  title: String!
  file: DjangoFileType!
  description: String!
  position: String!
  numberOfVacancies: Int!
  expiryDate: Date!
  isArchived: Boolean!
  publishedAt: Date!
}

"""
MajorResponsibilities(id, created_at, modified_at, created_by, modified_by, title, description, directive, slug)
"""
input MajorResponsibilitiesFilter {
  id: ID!
  slug: StrFilterLookup
  directive: DjangoModelFilterInput
  AND: MajorResponsibilitiesFilter
  OR: MajorResponsibilitiesFilter
  NOT: MajorResponsibilitiesFilter
  DISTINCT: Boolean
}

input MajorResponsibilitiesOrder {
  id: Ordering
  directive: Ordering
}

"""
MajorResponsibilities(id, created_at, modified_at, created_by, modified_by, title, description, directive, slug)
"""
type MajorResponsibilitiesType {
  id: ID!
  title: String!
  description: String!
  directive: DjangoModelType!
  slug: String!
}

"""
News(id, created_at, modified_at, created_by, modified_by, title, content, file, published_date, directive, slug, cover_image, status)
"""
input NewsFilter {
  name: String
  slug: String
  id: ID
  directive: ID = null
  status: StatusEnum
  AND: NewsFilter
  OR: NewsFilter
  NOT: NewsFilter
  DISTINCT: Boolean
}

input NewsOrder {
  id: Ordering
  title: Ordering
  status: Ordering
  publishedDate: Ordering
}

"""
News(id, created_at, modified_at, created_by, modified_by, title, content, file, published_date, directive, slug, cover_image, status)
"""
type NewsType {
  id: ID!
  title: String!
  file: DjangoFileType
  content: String!
  publishedDate: Date!
  directive: DjangoModelType
  slug: String!
  coverImage: DjangoFileType
}

enum Ordering {
  ASC
  ASC_NULLS_FIRST
  ASC_NULLS_LAST
  DESC
  DESC_NULLS_FIRST
  DESC_NULLS_LAST
}

"""
Partner(id, created_at, modified_at, created_by, modified_by, title, image, scope)
"""
input PartnerFilter {
  scope: PartnerScopeEnumFilterLookup
  AND: PartnerFilter
  OR: PartnerFilter
  NOT: PartnerFilter
  DISTINCT: Boolean
}

input PartnerOrder {
  id: Ordering
  scope: Ordering
}

enum PartnerScopeEnum {
  LOCAL
  GLOBAL
}

input PartnerScopeEnumFilterLookup {
  """Exact match. Filter will be skipped on `null` value"""
  exact: PartnerScopeEnum

  """Assignment test. Filter will be skipped on `null` value"""
  isNull: Boolean

  """
  Exact match of items in a given list. Filter will be skipped on `null` value
  """
  inList: [PartnerScopeEnum!]

  """Case-insensitive exact match. Filter will be skipped on `null` value"""
  iExact: PartnerScopeEnum

  """
  Case-sensitive containment test. Filter will be skipped on `null` value
  """
  contains: PartnerScopeEnum

  """
  Case-insensitive containment test. Filter will be skipped on `null` value
  """
  iContains: PartnerScopeEnum

  """Case-sensitive starts-with. Filter will be skipped on `null` value"""
  startsWith: PartnerScopeEnum

  """Case-insensitive starts-with. Filter will be skipped on `null` value"""
  iStartsWith: PartnerScopeEnum

  """Case-sensitive ends-with. Filter will be skipped on `null` value"""
  endsWith: PartnerScopeEnum

  """Case-insensitive ends-with. Filter will be skipped on `null` value"""
  iEndsWith: PartnerScopeEnum

  """
  Case-sensitive regular expression match. Filter will be skipped on `null` value
  """
  regex: PartnerScopeEnum

  """
  Case-insensitive regular expression match. Filter will be skipped on `null` value
  """
  iRegex: PartnerScopeEnum
}

"""
Partner(id, created_at, modified_at, created_by, modified_by, title, image, scope)
"""
type PartnerType {
  id: ID!
  title: String!
  scope: PartnerScopeEnum!
  image: DjangoFileType
}

"""
Procurement(id, created_at, modified_at, created_by, modified_by, title, description, file, published_date, expiry_date)
"""
input ProcurementFilter {
  id: ID!
  expiryDate: DateDateFilterLookup
  publishedDate: DateDateFilterLookup
  AND: ProcurementFilter
  OR: ProcurementFilter
  NOT: ProcurementFilter
  DISTINCT: Boolean
}

input ProcurementOrder {
  id: Ordering
}

"""
Procurement(id, created_at, modified_at, created_by, modified_by, title, description, file, published_date, expiry_date)
"""
type ProcurementType {
  id: ID!
  title: String!
  description: String!
  file: DjangoFileType
  publishedDate: Date!
  expiryDate: Date!
}

"""
Project(id, created_at, modified_at, created_by, modified_by, title, description, cover_image, department, start_date, end_date)
"""
input ProjectFilter {
  id: ID = null
  department_StrategicDirective: StrategicDirectivesFilter = null
  AND: ProjectFilter
  OR: ProjectFilter
  NOT: ProjectFilter
  DISTINCT: Boolean
}

input ProjectOrder {
  id: Ordering
}

"""
Project(id, created_at, modified_at, created_by, modified_by, title, description, cover_image, department, start_date, end_date)
"""
type ProjectType {
  id: ID!
  title: String!
  description: String!
  coverImage: DjangoFileType!
  department: DepartmentType
  startDate: Date
  endDate: Date
}

type Query {
  strategicDirectives(filters: StrategicDirectivesFilter, order: StrategicDirectivesOrder): [StrategicDirectivesType!]!
  strategicDirective(id: ID!): StrategicDirectivesType!
  majorResponsibilities(filters: MajorResponsibilitiesFilter, order: MajorResponsibilitiesOrder): [MajorResponsibilitiesType!]!
  majorResponsibility(id: ID!): MajorResponsibilitiesType!
  departments(filters: DepartmentFilter, order: DepartmentOrder): [DepartmentType!]!
  department(id: ID!): DepartmentType!
  procurements(filters: ProcurementFilter, order: ProcurementOrder): [ProcurementType!]!
  procurement(id: ID!): ProcurementType!
  jobVacancies(filters: JobVacancyFilter, order: JobVacancyOrder): [JobVacancyType!]!
  jobVacancy(id: ID!): JobVacancyType!
  project(id: ID!): ProjectType!
  projects(filters: ProjectFilter, order: ProjectOrder): [ProjectType!]!
  faqs(filters: FaqFilter, order: FaqOrder): [FaqType!]!
  faq(id: ID!): FaqType!
  resources(filters: ResourceFilter, order: ResourceOrder): [ResourceType!]!
  resource(id: ID!): ResourceType!
  partners(filters: PartnerFilter, order: PartnerOrder): [PartnerType!]!
  partner(id: ID!): PartnerType!
  blogs(filters: BlogFilter, order: BlogOrder): [BlogType!]!
  blog(id: ID!): BlogType!
  news(filters: NewsFilter, order: NewsOrder): [NewsType!]!
  newsItem(id: ID!): NewsType!
  highlights(filters: HighlightFilter, order: HighlightOrder): [HighlightType!]!
  highlight(id: ID!): HighlightType!
  radioProgram(filters: RadioProgramFilter, order: RadioProgramOrder): [RadioProgramType!]!
  enums: AppEnumCollection!
}

"""
RadioProgram(id, created_at, modified_at, created_by, modified_by, title, audio_file, published_date, type)
"""
input RadioProgramFilter {
  publishedDate: DateDateFilterLookup
  id: ID
  title: String
  type: RadioProgramTypeEnum
  AND: RadioProgramFilter
  OR: RadioProgramFilter
  NOT: RadioProgramFilter
  DISTINCT: Boolean
}

input RadioProgramOrder {
  id: Ordering
  title: Ordering
  publishedDate: Ordering
}

"""
RadioProgram(id, created_at, modified_at, created_by, modified_by, title, audio_file, published_date, type)
"""
type RadioProgramType {
  id: ID!
  title: String!
  audioFile: DjangoFileType!
  publishedDate: Date!
  type: RadioProgramTypeEnum!
}

enum RadioProgramTypeEnum {
  TUESDAY_PROGRAM
  TOGETHER_FOR_HUMANITY
}

"""
Resource(id, created_at, modified_at, created_by, modified_by, title, content, file, published_date, directive, slug, cover_image, type)
"""
input ResourceFilter {
  name: String
  slug: String
  id: ID
  directive: ID = null
  type: ResourceTypeEnum
  AND: ResourceFilter
  OR: ResourceFilter
  NOT: ResourceFilter
  DISTINCT: Boolean
}

input ResourceOrder {
  id: Ordering
}

"""
Resource(id, created_at, modified_at, created_by, modified_by, title, content, file, published_date, directive, slug, cover_image, type)
"""
type ResourceType {
  id: ID!
  title: String!
  file: DjangoFileType!
  content: String!
  publishedDate: Date!
  directive: DjangoModelType
  slug: String!
  coverImage: DjangoFileType
  type: ResourceTypeEnum!
}

enum ResourceTypeEnum {
  REPORT
  POLICY_AND_GUIDELINES
}

enum StatusEnum {
  DRAFT
  PUBLISHED
  ARCHIVED
}

input StrFilterLookup {
  """Exact match. Filter will be skipped on `null` value"""
  exact: String

  """Assignment test. Filter will be skipped on `null` value"""
  isNull: Boolean

  """
  Exact match of items in a given list. Filter will be skipped on `null` value
  """
  inList: [String!]

  """Case-insensitive exact match. Filter will be skipped on `null` value"""
  iExact: String

  """
  Case-sensitive containment test. Filter will be skipped on `null` value
  """
  contains: String

  """
  Case-insensitive containment test. Filter will be skipped on `null` value
  """
  iContains: String

  """Case-sensitive starts-with. Filter will be skipped on `null` value"""
  startsWith: String

  """Case-insensitive starts-with. Filter will be skipped on `null` value"""
  iStartsWith: String

  """Case-sensitive ends-with. Filter will be skipped on `null` value"""
  endsWith: String

  """Case-insensitive ends-with. Filter will be skipped on `null` value"""
  iEndsWith: String

  """
  Case-sensitive regular expression match. Filter will be skipped on `null` value
  """
  regex: String

  """
  Case-insensitive regular expression match. Filter will be skipped on `null` value
  """
  iRegex: String
}

"""
StrategicDirectives(id, created_at, modified_at, created_by, modified_by, title, description, cover_image, contact_person_name, contact_person_email, slug)
"""
input StrategicDirectivesFilter {
  id: ID!
  slug: StrFilterLookup
  AND: StrategicDirectivesFilter
  OR: StrategicDirectivesFilter
  NOT: StrategicDirectivesFilter
  DISTINCT: Boolean
}

input StrategicDirectivesOrder {
  id: Ordering
}

"""
StrategicDirectives(id, created_at, modified_at, created_by, modified_by, title, description, cover_image, contact_person_name, contact_person_email, slug)
"""
type StrategicDirectivesType {
  id: ID!
  title: String!
  description: String!
  contactPersonName: String
  contactPersonEmail: String
  coverImage: DjangoFileType
  slug: String!
  majorResponsibilities: [MajorResponsibilitiesType!]!
}